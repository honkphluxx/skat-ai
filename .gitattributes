# Git Bash on Windows hands a CRLF script to bash, which then reads the carriage
# return as part of every command and fails with "command not found" on line one.
*.sh text eol=lf
gradlew text eol=lf
